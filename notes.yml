name: mmm.com
project:
  - space:
      provider: docker
      location: local
      tomcat:
        image: tomcat
        ports: # reversed order
          - 8080: 8080
        mount: # !!!!!!!
          - app:/usr/local/tomcat/webapps